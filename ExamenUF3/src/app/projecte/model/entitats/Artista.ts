export class Artista {
    title: string;
    birth_date: string;
    death_date: string;

    constructor(title: string, birth_date: string, death_date: string) {
        this.title = title;
        this.birth_date = birth_date;
        this.death_date = death_date;
    }
} 