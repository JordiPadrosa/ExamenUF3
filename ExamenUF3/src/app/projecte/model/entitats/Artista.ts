export class Artista {
    birth_date: string;
    death_date: string;

    constructor(birth_date: string, death_date: string) {
        this.birth_date = birth_date;
        this.death_date = death_date;
    }
} 